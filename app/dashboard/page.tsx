import { auth } from '@/lib/auth'
import { headers } from 'next/headers'
import { redirect } from 'next/navigation'
export default async function DashboardPage() {
  const session = await auth.api.getSession({ headers: await headers() })
  if (!session?.user) redirect('/sign-in')
  return <main className="min-h-screen bg-background px-5 py-12 text-foreground lg:px-10"><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-5 border-b border-border pb-8 sm:flex-row sm:items-end"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Operator dashboard</p><h1 className="mt-3 font-display text-5xl font-bold uppercase">Welcome, {session.user.name}</h1><p className="mt-2 text-sm text-muted-foreground">{session.user.email}</p></div><a href="/" className="font-mono text-[10px] uppercase tracking-[0.16em] text-muted-foreground hover:text-primary">Back to arena</a></div><div className="mt-8 grid gap-4 md:grid-cols-3"><article className="border border-border bg-card p-5"><p className="font-mono text-[10px] uppercase tracking-[0.15em] text-muted-foreground">Registrations</p><p className="mt-4 font-display text-4xl font-bold">00</p><p className="mt-2 text-sm text-muted-foreground">Join an upcoming match to see it here.</p></article><article className="border border-border bg-card p-5"><p className="font-mono text-[10px] uppercase tracking-[0.15em] text-muted-foreground">Prize ledger</p><p className="mt-4 font-display text-4xl font-bold">₹0</p><p className="mt-2 text-sm text-muted-foreground">Verified winnings and withdrawals.</p></article><article className="border border-primary/40 bg-primary/10 p-5"><p className="font-mono text-[10px] uppercase tracking-[0.15em] text-primary">Fair-play status</p><p className="mt-4 font-display text-4xl font-bold">CLEAR</p><p className="mt-2 text-sm text-muted-foreground">Anti-cheat checks active on every event.</p></article></div><section className="mt-8 border border-border bg-card p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[0.16em] text-primary">Next actions</p><h2 className="mt-2 font-display text-3xl font-bold uppercase">Deploy your first squad</h2></div><a className="border border-primary px-4 py-3 font-mono text-[10px] uppercase tracking-[0.14em] text-primary hover:bg-primary hover:text-primary-foreground" href="/#tournaments">Browse events</a></div><div className="mt-6 grid gap-3 text-sm text-muted-foreground sm:grid-cols-3"><p className="border border-border p-4">Register solo or create a squad.</p><p className="border border-border p-4">Share roster links on WhatsApp or Telegram.</p><p className="border border-border p-4">Submit evidence if suspicious play appears.</p></div></section></div></main>
}
